import "package:flutter/widgets.dart";

class CocoIconsData extends IconData {
  const CocoIconsData(int codePoint, String fontFamily)
      : super(codePoint, fontFamily: fontFamily, fontPackage: "cocoicons");
}
